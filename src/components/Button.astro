---
import SecondForm from './first-form-page/SecondForm.astro'

interface Props {
  text: string
  href: string
}

const { text, href } = Astro.props
---

<div class="flex justify-end items-center mt-[1rem] mr-[5rem]">
  <a href={href}>
    <button class="bg-black text-white px-6 py-2 rounded flex">
      {text}
    </button>
  </a>
</div>